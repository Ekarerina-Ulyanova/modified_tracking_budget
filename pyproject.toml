[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "modified_tracking_budget"
version = "0.1.0"
description = "Simple budget tracking utilities"
requires-python = ">=3.8"
